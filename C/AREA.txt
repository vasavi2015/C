// Online C compiler to run C program online
#include <stdio.h>

int main() 
{
    int l,b,A;
    printf("Enter the value of length and breadth");
    scanf("%d%d",&l,&b);
    A=(l*b);
    printf("The value of area is %d",A);
    return 0;
}